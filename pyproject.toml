# pyproject.toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "datapact"
version = "0.0.1"
dependencies = [
    "databricks-sdk",
    "pyyaml",
    "loguru",
]

[project.scripts]
datapact = "datapact.main:main"
